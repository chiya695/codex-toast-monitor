# Codex Toast Monitor 安装说明

## 运行环境

- Windows 10 版本 19041 或更高版本，或 Windows 11。
- 已开启 Windows 开发者模式。
- ChatGPT/Codex 桌面应用的 Windows 通知保持开启。

## 安装步骤

1. 从发布页下载 `CodexToastMonitor-0.2.1-开发者模式.zip`，解压到一个不会被自动清理的目录。不要直接从压缩包内运行。
2. 打开“设置 > 系统 > 开发者”，开启“开发人员模式”。Windows 10 用户可在“设置 > 更新和安全 > 开发者选项”中找到此开关。
3. 在解压目录中右键 `安装-开发者模式.ps1`，选择“使用 PowerShell 运行”。如果系统阻止脚本，可在 PowerShell 中执行：

   ```powershell
   Set-ExecutionPolicy -Scope Process Bypass
   .\安装-开发者模式.ps1
   ```

   脚本只为当前用户注册程序包身份，不修改 ChatGPT/Codex 文件，也不需要证书。
4. 程序启动后，在“运行状态”页面按需启用飞书推送并填写飞书机器人的 Webhook 地址和签名 Secret，然后保存配置。

窗口最小化后会隐藏到系统托盘，双击托盘图标即可恢复。右键托盘图标可以打开窗口或彻底退出程序。在“运行状态”页面勾选“开机自动启动”并保存配置，可让程序在当前用户登录 Windows 时自动启动。

## 使用说明

程序启动后会读取当前通知列表建立基线，避免重复转发启动前的历史通知。之后每秒检查一次 ChatGPT/Codex 的新 Windows 通知，并在发现新通知时记录本地日志和发送飞书消息。

“运行日志”页面可以查看摘要和完整详情，也可以清理 30 天以前的日志或立即删除全部日志。日志文件位于：

```text
%LOCALAPPDATA%\CodexToastProbe\toast-events.jsonl
```

飞书配置和发送失败后的待发送队列同样只保存在本机：

```text
%LOCALAPPDATA%\CodexToastProbe\config.json
%LOCALAPPDATA%\CodexToastProbe\feishu-outbox\
```

## 卸载

在解压目录中运行 `卸载-开发者模式.ps1`，或在 PowerShell 中执行 `Get-AppxPackage -Name CodexToastMonitor | Remove-AppxPackage`。卸载不会删除之前保存的日志、配置和待发送队列；如需彻底清理，请在程序的“运行日志”页面删除日志，并手动删除 `%LOCALAPPDATA%\CodexToastProbe\`。

## 安全提示

Webhook 地址和签名 Secret 等同于机器人凭据，请勿提交到代码仓库、截图或发送给他人。开发者模式只影响本机应用安装和调试，不会修改 ChatGPT/Codex 的权限或配置。
